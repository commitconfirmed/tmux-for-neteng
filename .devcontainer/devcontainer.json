{
    "name": "TMUX For Network Engineers example",
    "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
    "customizations": {
        "vscode": {
            "settings": {
                "terminal.integrated.shell.linux": "/bin/bash"
            }
        },
            "extensions": [
                "vscode.docker",
                "vscode.git"
            ]
    },
    "postCreateCommand": "mkdir log && cat example-bashrc.txt >> ~/.bashrc && cp .tmux.conf ~/.tmux.conf && source ~/.bashrc"
}